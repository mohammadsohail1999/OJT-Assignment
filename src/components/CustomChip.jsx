import { Chip } from "@mui/material";
import React from "react";

const CustomChip = (props) => {
  //   const { label, color, varient, size } = props;

  return <Chip {...props} />;
};

export default CustomChip;
